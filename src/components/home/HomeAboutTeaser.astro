---
const { BASE = "/" } = Astro.props;
---

<section class="s-about about">
    <!-- Paste the homepage “about/teaser” block here.
       Update image/srcset/hrefs to use `${BASE}` if they start with `/`.
       e.g., <img src={`${BASE}images/...`} ... />
  -->
</section>

---
const { BASE = "/" } = Astro.props;
---

<section class="s-journal journal">
    <!-- Paste the “Recent writings / Journal” card grid here.
       Update each post link and image paths with `${BASE}`.
       If cards have category links, prefix those too.
  -->
</section>

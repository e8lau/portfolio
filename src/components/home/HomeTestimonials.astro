---
const { BASE = "/" } = Astro.props;
---

<section class="s-testimonials">
    <!-- Paste the testimonials section exactly as in the template.
       If it uses Swiper:
         - keep container classes like `testimonials__slider page-slider`
         - keep `.swiper-container`, `.swiper-wrapper`, `.swiper-slide`
         - keep `.swiper-pagination` element
       The global scripts in BaseLayout will initialize it.
  -->
</section>

---
// src/pages/projects/[slug].astro
export const prerender = true;

export async function getStaticPaths() {
    return []; // no dynamic pages yet; prevents the build error
}
---

<!-- Optional: simple message -->
